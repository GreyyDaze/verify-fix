{
  "name": "slots-booking-monitoring",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Checkly project for the slots-booking example: one Playwright Check Suite that logs in and books a slot.",
  "scripts": {
    "test": "checkly test",
    "test:preview": "checkly test --env-file=./.env",
    "deploy": "checkly deploy",
    "pw": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "checkly": "^9.5.0",
    "typescript": "^5.7.0",
    "@types/node": "^24.0.0"
  }
}
